Run `npm run dev` to start a development server for the webview content.

Run `npm run build` to build the webview content for production. This command should
be run before building and debugging the extension.